node_modules/
dist/

# Secrets / local env — NEVER commit (ElevenLabs key, etc.)
.env
.env.*
!.env.example

# Working design docs & contracts — kept local, not published until release
docs/

# Swift avatar build artifacts (Phase 4.2b)
avatar/.build/
avatar/.swiftpm/

# Live2D Cubism sample model (Free Material License — kept local, never redistributed;
# the real character is bought and lives in $FAMILIAR_HOME/character/)
avatar/characters/haru/

# Live2D Cubism Core — PROPRIETARY (Live2D Proprietary Software License). The Core
# may only be redistributed embedded in a finished application, NOT as standalone
# library files in a public repo. Users download the free Cubism SDK for Native and
# drop these in themselves — see avatar/Vendor/Live2DCore/README.md.
avatar/Vendor/Live2DCore/lib/libLive2DCubismCore.a
avatar/Sources/CubismLive2D/core/Live2DCubismCore.h
